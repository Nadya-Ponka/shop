/* import { HighlightDirective } from './highlight.directive';

describe('HighlightDirective', () => {
  it('should create an instance', (col) => {
    const directive = new HighlightDirective(this.col);
    expect(directive).toBeTruthy();
  });
}); */
